# - ค้นหาข้อมูล slot เวลาว่าง
# - จองสนาม(online) สำหรับ member
# - จองสนามแบบ(walk in) สำหรับ guest
# - มี stike และ raincheck
# - มี add-on (ใช้ได้ทั้ง member&guest)
# - มี bill สำหรับสรุปรายการ
# - มี pay ของ booking
# - ดูข้อมูลในระบบ (system) : users, booking-history, caddies, golfcarts